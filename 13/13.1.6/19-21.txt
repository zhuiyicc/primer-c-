19.你的Employee类需要定义它自己的拷贝控制成员吗？如果需要，为什么？如果不需要，为什么？实现你认为Employee需要的拷贝控制成员。
需要实现拷贝赋值函数，如果不实现，赋值时左侧对象id值与右侧对象id值相同，需要实现参数为Employee类引用的拷贝构造函数，理由如上。
20.解释当我们拷贝、赋值或销毁TextQuery和QueryResult类对象时会发生什么？
map，shared_ptr有拷贝构造函数及拷贝赋值函数，析构函数；
21.你认为TextQuery和QueryResult类需要定义它们自己版本的拷贝控制成员吗？如果需要，为什么？如果不需要，为什么？实现你认为这两
个类需要的拷贝控制操作。
不需要，如上。
